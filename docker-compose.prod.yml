services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: justeat_frontend_react_prod
    restart: unless-stopped
    expose:
      - "80"
    environment:
      - NODE_ENV=production
    networks:
      - justeat_network
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.docker.allownonrunning=true"
      - "traefik.http.routers.justeat-react.rule=Host(`justeat-react.swiftapp.ch`)"
      - "traefik.http.routers.justeat-react.entrypoints=websecure"
      - "traefik.http.routers.justeat-react.tls.certresolver=letsencrypt"
      - "traefik.http.routers.justeat-react.middlewares=sablier-justeat@file"
      - "traefik.http.services.justeat-react.loadbalancer.server.port=80"
      - "sablier.enable=true"
      - "sablier.group=justeat"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

networks:
  justeat_network:
    external: true
  traefik:
    external: true
